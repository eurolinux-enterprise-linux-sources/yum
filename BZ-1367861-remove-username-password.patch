diff -up yum-3.2.29/docs/yum.conf.5.old yum-3.2.29/docs/yum.conf.5
--- yum-3.2.29/docs/yum.conf.5.old	2016-10-18 11:03:11.681898489 +0200
+++ yum-3.2.29/docs/yum.conf.5	2016-10-18 11:03:31.748398444 +0200
@@ -437,14 +437,6 @@ username to use for proxy
 password for this proxy
 
 .IP
-\fBusername \fR
-username to use for basic authentication to a repo or really any url.
-
-.IP
-\fBpassword \fR
-password to use with the username for basic authentication.
-
-.IP
 \fBplugins \fR
 Either `0' or `1'. Global switch to enable or disable yum plugins. Default is
 `0' (plugins disabled). See the \fBPLUGINS\fR section of the \fByum(8)\fR man
diff -up yum-3.2.29/docs/yum.conf.5.old yum-3.2.29/docs/yum.conf.5
--- yum-3.2.29/docs/yum.conf.5.old	2016-12-07 11:19:39.616616117 +0100
+++ yum-3.2.29/docs/yum.conf.5	2016-12-07 11:20:14.524531763 +0100
@@ -849,16 +849,6 @@ If this is unset it inherits it from the
 
 
 .IP
-\fBusername \fR
-username to use for basic authentication to a repo or really any url.
-If this is unset it inherits it from the global setting
-
-.IP
-\fBpassword \fR
-password to use with the username for basic authentication.
-If this is unset it inherits it from the global setting
-
-.IP
 \fBcost \fR
 relative cost of accessing this repository. Useful for weighing one repo's packages
 as greater/less than any other. defaults to 1000
